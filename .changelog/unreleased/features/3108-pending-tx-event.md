- `[events]` Add callback for new transaction that added to the mempool, and publish pending tx event for each newly inserted transaction.
  ([\#3108](https://github.com/cometbft/cometbft/issues/3108))
