- `[mempool]` Add two metrics to measure how many times a tx was received (a
  histogram) and if a tx was received more than once (a counter).
  ([\#634](https://github.com/cometbft/cometbft/pull/634))